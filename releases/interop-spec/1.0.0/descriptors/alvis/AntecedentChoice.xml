<alvisnlp-doc author="Robert Bossy" date="2012-04-30" short-target="AntecedentChoice" target="org.bibliome.alvisnlp.modules.AntecedentChoice">
  <synopsis>
    <p>Biotopes-specific module: chooses an antecedent.</p>
  </synopsis>

  <module-doc>
    <description>
      <p>This module is project-specific and should be short-lived. It assumes a relation named <em>coreferences</em> containing tuples with at least one argument with role <em>Anaphora</em> and another with role <em>AntePreviousLowerTaxon</em>, a layer named <em>biAnaphora</em>.</p>
    </description>
  
    <param-doc mandatory="default: true" name="documentFilter" short-type="Expression" type="alvisnlp.corpus.expressions.Expression">
      <p>Only process documents that satisfy this filter.</p>
    </param-doc>
  
    <param-doc mandatory="default: true" name="active" short-type="Expression" type="alvisnlp.corpus.expressions.Expression">
      <p>This expression is evaluated as a boolean with the corpus as the context element, the module will process the document only if the result is true.</p>
    </param-doc>
      
    <param-doc mandatory="default: true" name="sectionFilter" short-type="Expression" type="alvisnlp.corpus.expressions.Expression">
      <p>Process only sections that satisfy this filter.</p>
    </param-doc>
  </module-doc>
</alvisnlp-doc>
